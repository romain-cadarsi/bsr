<section class="pt-0 confiance">
    <div class="container">
        <div class="col-lg-12 p-t-80 p-b-20">
            <div class="heading-text heading-section heading-gradient">
                <h2><span>Ils nous font confiance</span></h2>
            </div>
        </div>
        <ul class="grid grid-3-columns">
            {% for client in clients %}
            <li>
                {% include('components/lightboxClient.html.twig') with { 'image' : asset('images/upload/' ~ client.image), 'h' : '90'} %}
            </li>
           {% endfor %}
        </ul>
    </div>
</section>

